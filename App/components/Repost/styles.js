import StyleSheet from 'react-native-extended-stylesheet';

export default StyleSheet.create({
  container: {
    // marginBottom: 10,
    // width: '100%',
    marginTop: 5,
    borderLeftWidth: 1,
    borderColor: '#d9d8d7',
  },
});
