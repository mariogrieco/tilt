import StyleSheet from 'react-native-extended-stylesheet';

export default StyleSheet.create({
  container: {
    // marginBottom: 10,
    // width: '100%',
    marginTop: 10,
    borderLeftWidth: 3,
    borderColor: '#EBEBEB',
    borderRadius: 1.5,
  },
});
