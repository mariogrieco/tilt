// @flow
// Optional flow type
// import {RemoteMessage} from 'react-native-firebase';

export default async message => {
  // handle your message
  console.log(message);
  return Promise.resolve();
};
