// export default getTypeMessage = (post) => {
//   let hasPictures = false;
//   if (post.file_ids && post.file_ids.length > 0) {
//     if (post.metadata && post.metadata.files && post.metadata.files.length > 0) {
//       hasPictures = {
//         files: post.metadata.files,
//         ids: post.file_ids
//       }
//     }
//   }
//   if (hasPictures) return hasPictures
//   return si
// }
